{
  "name": "book-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute book-companion --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute book-companion --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
