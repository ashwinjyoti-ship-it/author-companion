{
  "name": "author-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "db:migrate:local": "wrangler d1 execute author-companion-db --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute author-companion-db --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
